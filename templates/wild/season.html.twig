{% extends './base.html.twig' %}

{% block title %}Série{% endblock %}

{% block body %}
    {% include './navbar.html.twig' %}

    <h1>Serie : {{ program }}</h1>

    <p>{{ season.description }}</p>


    {% for episode in episode %}

        <p>Episode {{ episode.number }} :<a href="{{ path('wild_episode', { 'id': episode.id }) }}">{{ episode.title }}</a></p>

    {% endfor %}

    <a href="{{ path('wild_showByProgram', { 'slug': program }) }}">
        Lien vers le program </a>


{% endblock %}